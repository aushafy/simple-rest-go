pipeline {

    agent any

    stages{

        stage("build"){
           echo "build stage ..."
        }

        stage("test"){
            echo "test stage ..."
        }

        stage("deploy){
            echo "deploy the app ..."
        }
    }
}
